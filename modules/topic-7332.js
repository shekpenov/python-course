export default {
  id: 'topic-7332',
  code: '7.3.3.2',
  title: 'Вложенные условия в Python',

  theory: `
<h2>Что такое вложенные условия?</h2>
<p>Вложенное условие — это условный оператор <code>if</code>, расположенный внутри другого условного оператора. Такая конструкция позволяет проверять дополнительные условия только тогда, когда выполняется внешнее условие. В реальных задачах вложенность часто необходима: например, скидка на товар предоставляется студентам, но только если сумма покупки превышает определённую сумму.</p>

<h2>Структура вложенных условий</h2>
<pre><code class="language-python">if внешнее_условие:
    # этот блок выполняется при истинности внешнего условия
    if внутреннее_условие:
        # выполняется при истинности обоих условий
    else:
        # внешнее истинно, внутреннее — ложно
else:
    # внешнее условие ложно</code></pre>
<p>Каждый уровень вложенности добавляет дополнительный отступ в 4 пробела. Глубокое вложение (более 2–3 уровней) ухудшает читаемость кода — в таких случаях лучше использовать составные условия с <code>and</code> / <code>or</code> или вспомогательные функции.</p>

<h2>Когда применять вложенные условия</h2>
<ul>
  <li>Когда одно условие является уточнением другого.</li>
  <li>Когда проверка второго условия имеет смысл только при выполнении первого.</li>
  <li>Когда логическое «и» между условиями не выражает смысл задачи так же наглядно.</li>
</ul>
<div class="warn"><strong>Внимание:</strong> следите за отступами! Неверный отступ изменит логику программы, и ошибка может не проявиться сразу — Python не укажет на неё как на синтаксическую.</div>
`,

  examples: [
    {
      title: 'Пример 1: Стоимость билета (возраст + день недели)',
      code: `vozrast = 14
den_nedeli = "суббота"  # "будний" или "суббота"

if vozrast < 18:
    print("Вы ребёнок.")
    if den_nedeli == "суббота":
        print("Цена билета: 200 тенге (выходной, детский)")
    else:
        print("Цена билета: 150 тенге (будний, детский)")
else:
    print("Вы взрослый.")
    if den_nedeli == "суббота":
        print("Цена билета: 500 тенге (выходной, взрослый)")
    else:
        print("Цена билета: 350 тенге (будний, взрослый)")`
    },
    {
      title: 'Пример 2: Система оценки с учётом посещаемости',
      code: `ball = 82
poseshaemost = 85  # процент посещаемости

if poseshaemost >= 75:
    print("Посещаемость в норме.")
    if ball >= 90:
        print("Итоговая оценка: Отлично")
    elif ball >= 70:
        print("Итоговая оценка: Хорошо")
    elif ball >= 50:
        print("Итоговая оценка: Удовлетворительно")
    else:
        print("Итоговая оценка: Неудовлетворительно")
else:
    print("Посещаемость недостаточна — оценка снижена.")
    if ball >= 90:
        print("Итоговая оценка: Хорошо (снижено из-за пропусков)")
    else:
        print("Итоговая оценка: Удовлетворительно (снижено из-за пропусков)")`
    }
  ],

  tasks: [
    {
      prompt: 'Дано число <code>x = 15</code>. Сначала проверьте, положительное ли оно. Если да — проверьте, чётное ли оно. Выведите одно из сообщений: <em>«Положительное чётное»</em>, <em>«Положительное нечётное»</em> или <em>«Неположительное»</em>.',
      starterCode: `# ваш код здесь
`,
      solution: `x = 15\nif x > 0:\n    if x % 2 == 0:\n        print("Положительное чётное")\n    else:\n        print("Положительное нечётное")\nelse:\n    print("Неположительное")`,
      hint: 'Внешний <code>if</code> проверяет знак (<code>x &gt; 0</code>). Внутри этого блока напишите ещё один <code>if / else</code> для проверки чётности через <code>x % 2 == 0</code>. Помните об отступах — вложенный блок сдвигается на 4 пробела дополнительно.',
      test: (out, code) => out.trim() === 'Положительное нечётное' && code.includes('if')
    },
    {
      prompt: 'Проверьте право на скидку: <code>vozrast = 20</code>, <code>student = True</code>. Если возраст меньше 26 — проверьте, студент ли. Студентам — скидка 20%, остальным молодым — 10%. Тем, кому 26 и более — скидки нет. Выведите процент скидки числом (например, <em>«Скидка: 20%»</em>).',
      starterCode: `# ваш код здесь
`,
      solution: `vozrast = 20\nstudent = True\nif vozrast < 26:\n    if student:\n        print("Скидка: 20%")\n    else:\n        print("Скидка: 10%")\nelse:\n    print("Скидка: 0%")`,
      hint: 'Внешнее условие: <code>vozrast &lt; 26</code>. Внутри него переменная <code>student</code> уже имеет тип <code>bool</code> — её можно использовать напрямую в <code>if student:</code>, без сравнения с <code>True</code>.',
      test: (out, code) => out.trim() === 'Скидка: 20%' && code.includes('if')
    },
    {
      prompt: 'Дано число <code>n = 36</code>. Проверьте: делится ли оно на 6? Если да — проверьте, делится ли на 9. Выведите: <em>«Делится на 6 и на 9»</em>, <em>«Делится только на 6»</em> или <em>«Не делится на 6»</em>.',
      starterCode: `# ваш код здесь
`,
      solution: `n = 36\nif n % 6 == 0:\n    if n % 9 == 0:\n        print("Делится на 6 и на 9")\n    else:\n        print("Делится только на 6")\nelse:\n    print("Не делится на 6")`,
      hint: 'Сначала проверьте <code>n % 6 == 0</code>. Только если это условие истинно, внутри добавьте проверку <code>n % 9 == 0</code>. Второй <code>if</code> пишется с отступом внутри первого.',
      test: (out, code) => out.trim() === 'Делится на 6 и на 9' && code.includes('if')
    }
  ]
};
